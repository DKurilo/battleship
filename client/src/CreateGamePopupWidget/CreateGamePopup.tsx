import * as React from 'react';

import styles from './CreateGamePopup.scss';

export const CreateGamePopup = (props:any) => 
  <div className={styles.CreateGamePopup}>
    Enter your name and your message to other players
  </div>
