import * as React from 'react';

import styles from './JoinPopup.css';

export const JoinPopup = (props:any) => 
  <div className={styles.JoinPopup}>
    Enter your name and small message. Then click join as guest or join as player...
  </div>
