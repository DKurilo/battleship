import * as React from 'react';

import styles from './Header.scss';

export const Header = (props:any) => 
  <div className={styles.Header}>
    &lt;- Back , Make game public and other things
  </div>
